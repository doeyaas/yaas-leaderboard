# CLAUDE.md — Project Guidelines

## Code Review Before Implementing

Before finalizing any implementation, run the `/simplify` skill to cross-check all changed code for correctness, reuse, quality, and consistency. This ensures nothing breaks and all correlations (schema ↔ types ↔ scrapers ↔ queries ↔ recompute) are accounted for.
